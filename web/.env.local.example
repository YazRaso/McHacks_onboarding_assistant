# Local environment variables for Backboard Dashboard
# Copy this into .env.local and set the value in development
BACKBOARD_API_KEY=your_backboard_api_key_here

# Backend API URL (NEXT_PUBLIC_ prefix makes it available in the browser)
NEXT_PUBLIC_API_URL=http://localhost:8000
